# BeyondAllRoyal — Implementation Checklist

Derived from [Requirements.md](Requirements.md) and design decisions. Do not edit Requirements.md.

---

## Project Setup

- [x] Create Unity project (2D, mobile build target)
- [x] Folder structure (Scripts, Prefabs, Scenes, ScriptableObjects, UI, Maps)
- [x] Input System configured (new Input System package; touch primary, mouse fallback via InputHelper)
- [x] Split `ProjectSetup` into three `partial class` files — it had grown past 1300 lines in one file across four clearly separable regions. `ProjectSetup.cs` now holds just the two menu entry points and the constants/name lists shared by the other two; `ProjectSetup.Assets.cs` holds Step 1 (ScriptableObject/prefab/sprite creation); `ProjectSetup.SceneWiring.cs` holds Step 2 (PlayScene's shop panel, HUD buttons, pause panel, NPC wiring). Pure reorganization — no behavior change, every method kept its name and signature

## Core Game Loop

- [x] Main game scene with fixed camera
- [x] Two-player symmetric layout in scene
- [x] Game state machine (Pregame → InGame → Victory/Defeat)
- [x] Win condition: detect HQ destruction, trigger end state
- [x] Return to the main menu after a win/loss — `GameManager.ReturnToMainMenu()` loads the `MainMenu` scene (also explicitly clears `BuildingRegistry`/`UnitRegistry`, since those are plain static lists a scene load wouldn't otherwise touch, so a later match doesn't start with stale entries); wired to a Main Menu button on the end screen via `BeyondAllRoyal → 2 - Setup Scenes`, which also registers the scene in Build Settings (required for the load). Previously this restarted `PlayScene` directly instead of going through the menu

## Main Menu

- [x] `MainMenu` scene (build index 0) with a title, an AI difficulty dropdown (Easy/Medium/Hard), a Singleplayer button, and a disabled Multiplayer placeholder button (`MainMenuController.cs`) — built from scratch (Camera, EventSystem with `InputSystemUIInputModule`, Canvas) by `BeyondAllRoyal → 2 - Setup Scenes` (`MainMenuSetup.CreateMainMenuScene`)
- [x] Singleplayer button writes the chosen difficulty/mode to `GameSetup` (a plain static class, not a `DontDestroyOnLoad` singleton — only needs to survive the one scene load) and loads `PlayScene`; defaults to Singleplayer/Medium so `PlayScene` can still be tested directly in the Editor without going through the menu
- [ ] Multiplayer — button present but non-interactive; not implemented (see Multiplayer (Post-MVP))
- [x] Dark purple UI theme (`UITheme.cs`) applied to `MainMenu` (baked in by `MainMenuSetup`) and `PlayScene`'s HUD/shop panel as part of `BeyondAllRoyal → 2 - Setup Scenes` (`ThemeSetup.ApplyPlaySceneTheme`) — re-applied every time that step runs, since it force-recolors whatever HUD/BuildingShopPanel already reference instead of skipping already-wired UI
- [x] Fixed the AI difficulty dropdown's option list rendering squished/overlapping ("Easy"/"Medium"/"Hard" bleeding into each other and the Singleplayer button below): `TMP_DefaultControls.CreateDropdown` sizes each option row for its own small default font, so bumping the item text to 28pt without also resizing the row height and the Template that contains all three rows left them too short for the text
- [x] Fixed the dropdown's caption/option text still rendering tiny after the above: `TMP_DefaultControls.CreateDropdown` enables TMP auto-sizing on both by default, which silently overrides a plain `.fontSize` assignment — auto-sizing is now explicitly disabled before setting the fixed 30pt/28pt sizes
- [x] Reduced the large empty gap between the main menu's title and its difficulty/Singleplayer/Multiplayer cluster by moving the whole cluster up, and pushed the Singleplayer/Multiplayer buttons further down so the dropdown's opened option list (which extends below the closed box) doesn't overlap the Singleplayer button
- [x] HUD button labels (Cancel/Demolish/Main Menu, shop cost labels, minimum-reserve label, main menu Singleplayer/Multiplayer) now use TMP auto-sizing instead of a fixed font size, so a label doesn't clip against its button's edges (seen with "Demolish") regardless of exact button width or text length
- [x] `ThemeSetup` now adds a background `Image` to `buildingInfoPanel`/`placementInfoPanel`/`endScreen`/the shop panel if one doesn't already exist, instead of silently skipping — a panel with only text/buttons and no backing image renders as text floating directly over the game world, which is hard to read
- [x] Bumped sizes further across the board, since there's plenty of unused screen space on a phone (dropdown caption/options 36/34pt, main menu buttons 460×110, shop category headers 32pt/56px-tall, shop rows 104px tall with an 84px icon/30pt name/26pt cost) — the shop panel also now stretches to the full canvas width (minus a small margin) instead of whatever fixed width it was manually given, since a categorized list with names needs more horizontal room than an icon grid did
- [x] Fixed poor building-info-panel visibility: `ThemeSetup` now enforces a 480×200 minimum size on `buildingInfoPanel`/`placementInfoPanel`/`endScreen` (only when point-anchored, so a deliberately stretch-anchored panel isn't distorted) and bumps their background to at least 95% opaque, since a panel sized to fit one small label was barely distinguishable from the game world behind it. `ThemeSetup`'s text styling also now sets auto-sizing (24-44pt) on every HUD text field it touches, not just color — they were all sized at whatever the Inspector's ~14-18pt default was
- [x] `MainMenuSetup.CreateMainMenuScene` and the HUD-button/minimum-reserve-slider steps in `ProjectSetup.cs` (`CreateHudChildButton`, `CreateMinimumReserveSlider`) now destroy and rebuild their GameObjects fresh every run instead of skipping once created — previously a re-run of `2 - Setup Scenes` silently did nothing for anything already generated (most visibly `MainMenu`, which needed to be manually deleted to pick up any change at all), so tuning sizes/positions/colors required deleting the old ones by hand first every time
- [x] Bumped the main menu dropdown further still (caption 44pt, options 40pt, 70px-tall rows) and the "AI Difficulty" label (46pt) — the previous bump never actually reached `MainMenu.unity` because it already existed and `CreateMainMenuScene` was skipping it (see above)
- [x] Fixed the minimum-reserve slider overlapping `buildingInfoPanel` in the same top-left corner: originally fixed by sibling z-order (`CreateMinimumReserveSlider` placing it first under the Canvas) plus disabling interaction while a building's info panel is showing, but the slider still visually rendered on top in practice — `HUD.OnBuildingSelected` now hides it outright (`gameObject.SetActive(false)`, both slider and its label) whenever `buildingInfoPanel` is open, instead of only disabling it
- [x] Shop icons now show each building's own tint (`BuildingShopPanel.Start` reads the prefab's `SpriteRenderer.color`, the same source `ProjectSetup.CreateBuildingPrefab` sets at creation time) instead of rendering plain white — the generated icon sprite itself is a colorless badge shape, so the color has to come from the prefab, not the sprite
- [x] Removed the standalone production panel that covered the play field when a production building was selected (it was largely redundant with `buildingInfoPanel`, which already shows the selected building's name) — `HUD` permanently force-hides the old panel GameObject on `Awake` instead, and the Pause/Resume Production button now lives inside `buildingInfoPanel` itself (bottom-left corner, mirroring Demolish at bottom-right; `ProjectSetup.CreateToggleProductionButton`, added via the same destroy-and-rebuild `CreateHudChildButton` helper used for Cancel/Demolish/Main Menu, extended to also wire a dynamic label field for the "Pause"/"Resume" text)
- [x] Bumped the minimum-reserve slider from 320×44 to 420×56 (its label shifted right to 470px to stay clear); fixed the building-info energy bar looking like a draggable control despite being a read-only display — `ThemeSetup.StyleSlider` now takes an `interactive` flag that, when false, sets `Slider.interactable = false` and hides the handle graphic entirely instead of just coloring it, so a progress-only bar doesn't visually invite dragging (`energyBar` uses this; `minimumReserveSlider`, an actual control, keeps its handle)
- [x] Fixed HUD button/shop-row text rendering much smaller than its font-size cap despite generously-sized buttons/rows: TMP word-wrapping was on by default, so a label too wide for its button (e.g. "Pause Production") wrapped onto two lines instead of just shrinking horizontally, and auto-sizing then had to shrink much further to fit *both* lines vertically too — every button label and shop row's name/cost now sets `textWrappingMode = NoWrap` so it only ever shrinks as far as fitting on one line actually requires. Buttons also bumped bigger to match (Cancel/Demolish 220×88, Pause/Resume Production 280×88, Main Menu 320×100, shop rows 128px tall with a 100px icon/38pt name/32pt cost, category headers 38pt/64px) and `buildingInfoPanel`'s enforced minimum size grew to 640×280 so its two bottom-corner buttons fit side by side without touching
- [x] Master volume slider (0-100%, below the Multiplayer button) writes to `SoundSettings.Volume`, a plain static class that persists the value with `PlayerPrefs` (unlike `GameSetup`, which only needs to survive one scene transition, volume should survive between separate play sessions) and applies it globally through `AudioListener.volume` — every sound effect spawns its own temporary `AudioSource` rather than sharing one, so scaling the single listener is simpler than reaching into each one. `SoundSettings.Apply()` runs in both `MainMenuController.Awake()` and `GameManager.Awake()`, since `AudioListener.volume` doesn't reload from `PlayerPrefs` by itself on a fresh session (`MainMenuSetup.BuildVolumeSlider`)

## Testing

- [x] `TestScene` — a duplicate of `PlayScene` (via `AssetDatabase.CopyAsset`, so it inherits every manually-wired reference for free) created as part of `BeyondAllRoyal → 2 - Setup Scenes` (`TestSceneSetup.CreateTestScene`), with a `TestSceneBootstrap` component that pre-places a starter loadout (5x Metal Factory, 3x Tesla Tower, 1x Barracks, 1x GunRange — economy-heavy so testing doesn't stall waiting on metal) for both Player and NPC once the match starts (free, not paid for through `ResourceManager`, same as how `MapGrid.PlaceHQs` places the HQ) — registered in Build Settings; open it directly and hit Play instead of building an economy up from scratch every time. Re-running that step now always deletes and re-copies `PlayScene`'s current state fresh (rather than reusing whatever was captured the first time TestScene was created), so TestScene can't silently go stale relative to PlayScene's own HUD/theme/wiring changes — only the starter-building loadout is preserved across a re-run (re-read from `StarterBuildingNames`), any other TestScene-only manual customization would need to be re-applied

## Map System

- [x] Building slot grid — each side has ~63 free slots (9 cols × 8 rows; back 3 rows reserved for HQ, 3×3); columns changed from 8 to 9 so the HQ's 3-wide footprint centers exactly instead of sitting one slot off-center — re-run `ProjectSetup` Step 1 to update the existing `DefaultMap` asset
- [x] Fixed both HQ placement (`MapGrid.SpawnHQ`) and `TestSceneBootstrap`'s starter-building placement silently ignoring `TryPlaceBuilding`'s return value — a failed placement (map layout misconfigured, or a prefab missing its `Building` component) left the instantiated GameObject alive but never actually on the grid, with `GridOrigin` stuck at its `(0,0)` default; if that building later died, it would incorrectly vacate whatever real building occupies grid slot (0,0) (see `MapGrid.RemoveBuilding` fix below). Both now destroy the orphaned instance and log an error/warning on failure instead of leaving it dangling
- [x] `MapGrid.RemoveBuilding` (now takes the `Building` itself rather than a bare origin/size) only vacates a slot if it's actually occupied by that building, instead of blindly clearing whatever's at its `GridOrigin` — closes the gap the fix above depends on
- [x] Buildings can occupy variable slot counts
- [x] Map layout loaded from ScriptableObject so layouts are swappable
- [x] Default map: two-lane Clash Royale-style layout

## Resources

- [x] Global Metal counter per player; income trickles over time
- [x] Per-building Energy buffer (fixed capacity, passive trickle fill rate)
- [x] Construction/production triggers only when required Energy has accumulated
- [x] Metal paid upfront when construction/production starts
- [x] Metal displayed in HUD
- [x] Energy buffer shown per selected building (script written; wire energyBar Slider in Inspector)
- [x] Shared minimum metal reserve (`ResourceManager.MinimumMetalReserve`) settable via a HUD slider; enforced against unit-production spending for both sides (`TrySpendMetalAboveReserve`) and used as a floor by the NPC's building-placement reserve — does not gate the player's own building costs

## Buildings

- [x] Base `Building` class (health, energy buffer, owner, slot size)
- [x] HQ building (win-condition target, metal + energy generation, self-defense auto-attack so a rush can't end the game instantly — default `attacksPerSecond` = 5/3 (≈1.67), cut by a factor of 3 from the original 5 since it was firing way too fast; needs `attackDamage`/`attackRange`/`attacksPerSecond`/`energyCostPerShot` populated on the `HQData` asset; re-run `ProjectSetup` Step 1)
- [x] Base `ProductionBuilding` class (continuous production loop, start/stop)
- [x] Changed `ProductionBuilding` to spend a unit's metal cost only once its energy buffer is actually full (`TrySpendMetalAboveReserve` retried every frame until the pool can afford it), instead of reserving the metal upfront the moment production started — the old upfront-reservation design had no refund path, so demolishing the building or pausing production while metal was reserved just silently lost it. Removed the now-unnecessary `metalReserved` flag entirely
- [x] 5 unit-production buildings (data + prefabs created)
- [x] Machinegun Turret (auto-attacks; counters Soldier, Heavy Gunner)
- [x] Railgun Turret (auto-attacks; counters Hovercraft, Heavy Tank)
- [x] Tesla Tower (injects energy into adjacent non-full buildings)
- [x] Metal Factory (passive metal income)
- [x] Defensive towers drain energy buffer on each shot; stop firing when empty
- [x] All buildings passively trickle-fill their own energy buffer
- [x] Building placement on slots — player input (BuildingPlacer + BuildingGhost); ghost/placement footprint is centered on the tapped cell (`MapGrid.GetPlacementOrigin`) instead of treating it as the top-left corner, so it now visually spans exactly the cells that will be affected; `BuildingGhost` forces a full-bleed solid sprite (not whatever "badge"-style icon sprite happened to be assigned in the Inspector, which had lots of transparent padding and made the ghost look tiny), forces `SpriteRenderer.drawMode = Simple`, and detaches from any scene parent on `Awake`, so its size can't be thrown off by scene-side Inspector state
- [x] Fixed the ghost defaulting to the bottom-left slot on mobile whenever the player wasn't actively touching: `InputHelper.TapPosition()` falls back to screen `(0,0)` when nothing is pressed and there's no mouse hardware (the normal case on a phone), and `BuildingPlacer` was updating/showing the ghost every frame regardless of touch state. It now only shows the ghost while a finger/mouse button is held (`InputHelper.IsPressed()`), hides it otherwise, and places on release (`InputHelper.TapEnded()`) instead of on initial touch — you drag to preview a slot, lift to commit, matching how there's no such thing as "hovering" without touching on a touchscreen
- [x] Opening the shop panel always cancels any in-progress building placement (`HUD.ToggleShop`), so a previously-selected building doesn't linger as a ghost underneath the menu; opening the shop also deselects any selected building (and selecting a building closes the shop, `HUD.OnBuildingSelected`), so the shop and building-info panels can't be open at the same time competing for screen space
- [x] Fixed selecting a building from the shop instantly placing it at whatever the release happened to land on: closing the shop panel (part of the same click) removed the UI element that would otherwise have blocked `BuildingPlacer`'s place-on-release check, so the very release that triggered the shop button's own `onClick` was immediately reinterpreted as a "place here" release. `BuildingPlacer` now ignores all input after a shop selection until the player begins a brand-new press (`awaitingFreshPress`), so a placement can never fire from the same press/release cycle that selected the building
- [x] Dragging a placement ghost onto the enemy's side of the map (`MapGrid.IsOnSide`) shows no ghost at all instead of an invalid/red one, and releasing there cancels the whole placement instead of silently doing nothing
- [x] Shop buttons are only interactable if the player can currently afford them (`BuildingShopPanel.Update`, checked against `ResourceManager.PlayerMetal` every frame the shop is open); Unity's built-in disabled-button tint blocks the click, so unaffordable buildings can't be selected at all
- [x] Fixed unaffordable shop rows being visually indistinguishable from affordable ones: `UITheme.ApplyButtonColors` had `colors.disabledColor` set equal to the normal fill color, so Unity's non-interactable tint was a no-op — a genuinely dimmed color (`UITheme.Dim`, blended toward `Background` rather than a flat tone, so it stays a strong dim regardless of the button's own base color) fixes the background for every themed button, and `BuildingShopPanel` now also fades the icon to 35% alpha and switches the name/cost text color (`UITheme.DisabledText`/`UITheme.Warning`, the latter a coral-red for the cost specifically) when a building can't currently be afforded, so it reads clearly at a glance rather than as a faint background shift. Moved `UITheme.cs` from `Scripts/Editor/` to `Scripts/UI/` so this runtime script could reference it — nothing in the palette was actually Editor-only, it just hadn't been needed at runtime before
- [x] NPC builds continuously from a randomly-assigned set of 3 production types as metal allows (see NPC / AI)
- [x] Any building except HQ can be voluntarily demolished to free its slot (`Building.Demolish()`, HUD Demolish button shown when a non-HQ building is selected; `HQ.Demolish()` refuses as a second line of defense)
- [x] Fixed Demolish/Pause Production appearing not to work: `BuildingSelector`/`BuildingPlacer` never checked whether a tap landed on a UI element, so clicking those HUD buttons also registered as a world-space tap and immediately deselected the building right after the action ran. Both now skip via `InputHelper.TapHitInteractiveUI()` when the tap hit an interactive element (Button/Slider) specifically — an initial blanket `IsPointerOverGameObject()` check caused a regression where the info panel couldn't be closed at all for production buildings (they show an extra `productionPanel`, covering enough screen area that a "tap elsewhere to deselect" often landed on that panel's own background and got swallowed too); the precise Selectable-only check lets taps on passive panel backgrounds still reach the world
- [x] Pause/Resume Production button is now explicitly hidden for anything that isn't a `ProductionBuilding` (Tesla Tower, Metal Factory, towers) instead of only relying on the parent panel's visibility
- [x] Fixed `Building` having no guard against re-entrant death, unlike `Unit` (which already guards with `isDead` — see Units): a chained explosion's splash damage (or a double-tapped Demolish) could call `TakeDamage`/`Demolish` on an already-dying building again before `Destroy()` had actually removed it. For HQ specifically this meant `GameManager.OnHQDestroyed` could fire twice, and if both HQs died in the same frame, the second call would silently overwrite whether the match ends in Victory or Defeat. `Building` now funnels both death paths (`TakeDamage`, `Demolish`) through a single non-virtual `DestroyBuilding()` guarded by an `isDestroyed` flag, checked *before* any subclass override (e.g. HQ's) runs its own logic — checked in `TakeDamage` and in `DestroyBuilding`, not just in the virtual `OnDestroyed()` hook a subclass could otherwise slip past
- [x] `BuildingPlacer`/`BuildingSelector` now also explicitly check `GameManager.IsPaused` before processing input — belt-and-suspenders alongside the pause overlay's own full-screen click-blocker (see Core Game Loop), which already prevented this in the normal case but depended entirely on `HUD.pausePanel` being wired correctly

## Units

- [x] Base `Unit` class (health, damage, attack range, speed, owner)
- [x] Autonomous movement: target priority is enemy units on our own side of the map (home defense) > buildings in attack range > units in attack range > buildings out of range > units out of range (units still fight back when attacked, not just when nothing else is around)
- [x] When assaulting a building, units keep advancing between attacks instead of holding at the range boundary — stationary buildings can't be overrun by mistake, so there's no downside to closing the distance; they stop once they reach the building's own footprint edge (`UnitAI.BuildingStoppingDistance`). Unit-vs-unit combat still holds at range, unchanged
- [x] Auto-attack when in range (no player input)
- [x] Shoot sound effect on every attack (`Unit.PlayShootSfx`, `GameSettings.unitShootSfx`) — same clip for all units, pitched by the shooting unit's own `maxHealth` so bigger units sound deeper and smaller ones sound higher (`shootPitchReferenceMinHealth`/`MaxHealth` → `shootPitchForMinHealth`/`MaxHealth`, defaults 50/350 → 1.3/0.7) — since audio can't be procedurally generated like the sprites, `ProjectSetup` Step 1 auto-wires it from `Assets/Resources/shot.mp3` by convention if present (leaves it alone otherwise, so a manual Inspector assignment isn't clobbered); re-run Step 1 to seed the pitch fields and pick up the clip
- [x] Death / cleanup on health reaching zero
- [x] Death explosion (`Unit.Explode`): flat splash damage (no counter multiplier) to all units in range — friendly and enemy — but only to enemy buildings; damage/radius scale with the dying unit's own `maxHealth` via `GameSettings.explosionDamageFraction`/`explosionRadiusPerHealth` (defaults 0.3, 0.01); plays a placeholder expanding ring (`ExplosionSpawner`/`ExplosionEffect`) and `GameSettings.explosionSfx`, auto-wired by `ProjectSetup` Step 1 from `Assets/Resources/splosion.mp3` by convention if present — re-run Step 1 to seed the damage/radius fields and pick up the clip
- [x] Fixed a Unity (not just game) crash from unit explosions: a chain reaction could hit an already-dying unit again before `Destroy` removed it, re-entering `Die()` → `Explode()` indefinitely until the stack overflowed — most likely to happen right as a match ends and a cluster of units die together. `Unit` now guards with `isDead` so a unit can only die once
- [x] 5 unit types defined (Soldier, Heavy Gunner, Explosive Specialist, Hovercraft, Heavy Tank); default move speeds set to 1/4 of the original design values — re-run `ProjectSetup` Step 1 to update existing `UnitData` assets
- [x] Unit metal costs lowered and energy costs (build time) raised a bit across the board — metal is the tighter bottleneck since it's a shared pool across all production buildings, energy isn't — re-run `ProjectSetup` Step 1 to update existing `UnitData` assets

## Counter System

- [x] Counter chart defined for 5 units + 2 towers
- [x] Combat damage modifier applied based on counter relationships
- [x] Fixed `CounterChartData.GetResult` guarding against the wrong thing: it validated the attacker/defender values against `Size` (derived from the live `EntityType` enum, which — being cast from a valid enum value — is essentially always in range), but never checked whether the serialized `matrix` array itself was actually `Size * Size` long. A chart asset saved before a new `EntityType` was added would throw `IndexOutOfRangeException` on the lookup, in exactly the scenario the existing error message claimed to catch ("did you add a new EntityType without re-running Initialize Default Counter Chart"). Now checks `matrix.Length` directly

## NPC / AI

- [x] NPC resource accumulation (same rules as player)
- [x] NPC is randomly assigned 3 of the 5 production building types at match start (`NPCController.AssignRandomBuildingTypes`, picked from `allProductionBuildingTypes` — auto-wired with all 5 by `BeyondAllRoyal → 2 - Setup Scenes`), and cycles through them, placing new instances only once metal has a surplus over a reserve threshold (previously it out-built its own economy and never had metal left to actually produce units)
- [x] NPC's metal reserve threshold scales with its own base: `(sum of metalCostPerUnit across active production buildings) * metalReserveMultiplier` (default 1.1) — grows automatically as more buildings go up, instead of a flat number
- [x] NPC fills free slots back-to-front (`MapGrid.TryGetFreeSlot` searches rows closest to its own HQ first), so new buildings tuck in behind existing ones instead of exposing themselves at the front line
- [x] NPC keeps all placed production buildings continuously producing
- [x] NPC occasionally places an economy building (Metal Factory, Tesla Tower, ...) instead of a production building — `economyBuildingTypes`/`economyBuildChance` (default 25%) on `NPCController`, still needs at least Metal Factory assigned in the Inspector
- [x] NPC's reserve threshold is floored by the shared `ResourceManager.MinimumMetalReserve` (set via the HUD slider), on top of its own dynamic per-building calculation
- [x] NPC forces an economy building through (ignoring the reserve) if `forceEconomyBuildAfterSeconds` (default 15s) passes without placing anything at all, so it can't get stuck never building
- [x] AI difficulty (chosen on the main menu, `GameSetup.Difficulty`) scales the NPC's economy pacing — `NPCController.ApplyDifficulty` multiplies `placementCheckInterval`/`metalReserveMultiplier`/`economyBuildChance`/`forceEconomyBuildAfterSeconds` by an Easy/Medium/Hard factor at `Awake`; unit/building stats stay symmetric between Player and NPC

## UI / Mobile

- [x] Touch + mouse input (legacy Input API; migrate to Input System later)
- [x] Building shop panel (BuildingShopPanel.cs); closes automatically once a building is selected so it doesn't block the placement view; grouped into Economy / Units / Defense categories with a heading per group, each building shown as a row (icon, name, cost) rather than an icon-only grid — fully rebuilt from `ProjectSetup.ShopCategories` every time `BeyondAllRoyal → 2 - Setup Scenes` runs (`ProjectSetup.PopulateShopPanel`), since the panel's whole layout can change between runs, unlike a simple add-if-missing list; icon/name/cost values are set at runtime from `BuildingData` (`BuildingShopPanel.Start`), not baked in, so they stay in sync without re-running Setup
- [x] Shop icons are tinted to match each building's own color instead of rendering plain white: the generated sprite itself is a colorless badge shape, and a building's actual color comes from its prefab's `SpriteRenderer` tint (set once in `ProjectSetup.CreateBuildingPrefab`) — `BuildingShopPanel.Start` now reads that same tint from `entry.prefab` and applies it to the icon `Image`, so there's one color source instead of two that could drift apart
- [x] Building placement with ghost preview (BuildingPlacer + BuildingGhost)
- [x] Tap placed building to select it (BuildingSelector)
- [x] Production start/stop per selected building — Pause/Resume Production button lives inside `buildingInfoPanel` itself (see Feedback / Polish for the removal of the old standalone production panel)
- [x] Win/lose screen (HUD.ShowEndScreen — needs UI GameObject in scene) with a Main Menu button (see Core Game Loop)
- [x] Metal cost shown during placement (placementInfoPanel in HUD; wire in Inspector)
- [x] Pause button (top-right corner, always visible during a match) opens a full-screen pause overlay with Resume and Main Menu options — `GameManager.Pause()`/`Resume()` drive it via `Time.timeScale` (0 while paused), which freezes every gameplay system for free since they all time themselves off `Time.deltaTime`, without needing an `IsPaused` check threaded through each one; the overlay's dimmed background also carries a bare `Button` (no listener) so `InputHelper.TapHitInteractiveUI()` treats a tap anywhere on it as hitting UI and blocks the world-tap handling underneath — otherwise tapping the dimmer (not directly on a button) would still register as a building select/place tap despite being paused. Hidden once the match ends (`HUD.ShowEndScreen`), since pausing a decided match doesn't make sense. `GameManager.Awake()`/`ReturnToMainMenu()` both reset `Time.timeScale = 1f` defensively, since it's a global engine static that would otherwise leak a pause into the next scene (`BeyondAllRoyal → 2 - Setup Scenes` → `ProjectSetup.CreatePauseButton`/`CreatePausePanel`)
- [x] Fixed the pause panel able to show even when `GameManager.Pause()` silently no-ops (it only takes effect during `GameState.InGame`, so a tap on the Pause button during the brief `Pregame` window at scene start — before `MapGrid` finishes generating — showed the "Paused" overlay while `Time.timeScale` was still 1, i.e. gameplay kept running invisibly behind it): `HUD.OnPauseClicked` now only shows `pausePanel` when `GameManager.IsPaused` is actually true after calling `Pause()`, instead of unconditionally
- [x] `SoundSettings.Volume`'s setter now calls `PlayerPrefs.Save()` immediately instead of relying on Unity's implicit flush on a clean app quit — mobile OSes routinely kill a backgrounded app without one, which would silently drop the last volume change; cheap here since volume only changes on user input, not per-frame

## Feedback / Polish

- [x] Health bar on all units and buildings (red → green gradient, auto-generated by ProjectSetup)
- [x] Production bar on production buildings shows the raw energy buffer relative to its capacity (not just progress toward the current unit, which used to freeze while paused or waiting on a metal reservation, and could jump instantly when there was already a surplus stored), with a yellow indicator tick marking the energy threshold needed for one unit (`HealthBar.SetIndicator`) — re-run `ProjectSetup` Step 1 to backfill the indicator onto already-created production building prefabs. Also fixed the bar being frozen at its stale prefab default (full bar, indicator at 100%) for the entire construction phase — it was gated behind `IsConstructed`, so it only got its first real update the instant construction finished, snapping down and looking exactly like a unit had just been produced (none was)
- [x] Fixed the energy bar only ever appearing to fill when energy income outpaced metal income: `TickProduction` was draining a little energy out of the buffer every single frame as it trickled in, instead of letting the buffer accumulate up to `energyCostPerUnit` first and consuming it all at once — same total production time, but now the bar actually visibly fills toward the indicator instead of sitting pinned near zero
- [x] Cancel placement with Escape/right-click (desktop) or the new Cancel button under `placementInfoPanel` (touch — Escape/right-click don't exist on mobile, so placement was previously uncancelable there; automated via `BeyondAllRoyal → 2 - Setup Scenes`)
- [x] Placeholder sprite art for all units and buildings (procedurally generated, `Assets/Sprites/`; higher native resolution for multi-tile buildings so they stay crisp when stretched)
- [x] Buildings cycle between two sprite frames on a timer (`Building.spriteCycleInterval`); same sprite doubles as the build-menu icon
- [x] Units swap to a brief "shooting" sprite on attack, then revert to idle
- [x] Every attack (units, towers, HQ) draws a placeholder beam from attacker to target, blue for the player and red for the NPC (`AttackBeamSpawner`, `Assets/Scripts/Effects/`) — explicitly a placeholder, swap out later
- [x] Wire `BuildingShopPanel.ShopEntry.icon` to each shop button — `BeyondAllRoyal → 2 - Setup Scenes` sets it directly when creating each entry (`ProjectSetup.PopulateShopPanel`), colored to match the building's own prefab tint (`BuildingShopPanel.Start`)
- [x] Minimum-metal-reserve HUD slider — created + wired under HUD's Canvas by `BeyondAllRoyal → 2 - Setup Scenes` (`ProjectSetup.CreateMinimumReserveSlider`), styled by the same step (`ThemeSetup.ApplyPlaySceneTheme`)

## Performance

- [x] UnitRegistry — O(1) unit lookup, replaces FindObjectsByType per frame
- [x] BuildingRegistry — O(1) building lookup, replaces FindObjectsByType per frame
- [x] `InputHelper.TapHitInteractiveUI()` no longer allocates a new `PointerEventData`/`List<RaycastResult>` on every call — it runs from multiple `Update()`s on every input frame (`BuildingPlacer`, `BuildingSelector`), so that was a steady stream of GC churn for no reason on mobile. Reused static buffers instead
- [x] Every unit was doing up to 3 full O(n) registry scans per frame (nearest enemy unit for the home-defense check, nearest enemy building, nearest enemy unit again) using `Vector2.Distance`'s `sqrt` — fine at small unit counts, but scales badly in a large battle. `UnitAI` now splits target *selection* (the expensive scan) from target *acting* (movement/attack against whatever was last picked): selection reruns every `RetargetInterval` (0.15s, phase-shifted per unit at spawn so they don't all rescan on the same frame) or immediately if the current target dies, cutting the scanning cost roughly 9x at 60fps, while acting still runs every frame so combat itself stays fully responsive. All distance comparisons across `UnitAI` and `Building` (`InjectEnergyIntoNearby`, `FindNearestEnemyUnitInRange`, `IsWithinRange`) also switched from `Vector2.Distance` to squared-distance comparisons, which give identical results without the `sqrt`. `Building`'s own auto-attack targeting (`TickAutoAttack`, used by towers/HQ) didn't need the same staggering — it already only rescans when the current target dies or leaves range, not every frame

## Multiplayer (Post-MVP)

- [ ] Networking layer
- [ ] Lobby with map selection
- [ ] 1v1 online matchmaking

## Version Control

- [x] Unity-appropriate `.gitignore` (Library/Temp/Build artifacts, IDE cruft, APKs, keystores)
- [x] README with setup steps and project layout
- [x] Pre-commit hook scanning staged changes for leaked credentials/tokens (`.githooks/pre-commit`; enable via `git config core.hooksPath .githooks`)
- [x] `git init` / first commit / remote ([Adyrem/BeyondAllRoyal](https://github.com/Adyrem/BeyondAllRoyal), public)
